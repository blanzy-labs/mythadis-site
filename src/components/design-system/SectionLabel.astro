<p class="ds-section-label"><slot /></p>

<style>
	.ds-section-label {
		max-width: 100%;
		margin: 0 0 16px;
		color: var(--ds-blue);
		font-family: var(--ds-font-mono);
		font-size: 0.72rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		overflow-wrap: anywhere;
	}

	@media (max-width: 420px) {
		.ds-section-label {
			letter-spacing: 0.06em;
		}
	}
</style>
