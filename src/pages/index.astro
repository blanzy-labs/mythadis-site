---
import Button from "../components/design-system/Button.astro";
import NetworkMotif from "../components/design-system/NetworkMotif.astro";
import SectionLabel from "../components/design-system/SectionLabel.astro";
import SiteHeader from "../components/design-system/SiteHeader.astro";
import StatusLabel from "../components/design-system/StatusLabel.astro";
import SurfacePanel from "../components/design-system/SurfacePanel.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import { statusSummary } from "../data/current-state";

const navItems = [
	{ label: "Vision", href: "#vision" },
	{ label: "Why Mythadis", href: "/why-mythadis/" },
	{ label: "Proof", href: "/proof/" },
	{ label: "Current State", href: "/current-state/" },
	{ label: "Collaborate", href: "#collaborate" },
];

const fragmentationNodes = [
	"Knowledge",
	"Institutions",
	"AI",
	"People",
	"Machines",
	"Evidence",
];

const flowSteps = [
	{
		name: "Mission",
		description: "Define the problem, scope, participants, and proof requirements.",
	},
	{
		name: "Data Parcels",
		description: "Prepare approved data as bounded units for distributed work.",
	},
	{
		name: "Discovery",
		description: "Find relevant knowledge, resources, and participating systems.",
	},
	{
		name: "Agents",
		description: "Execute bounded tasks across participating machines and operators.",
	},
	{
		name: "Evidence",
		description: "Validate and record observations, results, and supporting material.",
	},
	{
		name: "Annals",
		description: "Preserve durable mission history, provenance, and validated outcomes.",
	},
];

const missionSwarms = [
	"Longevity",
	"Pollution",
	"Climate",
	"Public science",
	"Knowledge preservation",
];

const collaborators = [
	"Engineers",
	"Researchers",
	"Institutions",
	"Advisors",
	"Early testers",
];

const statusModel = [
	{
		label: "Working today",
		tone: "amber",
		items: "Early private multi-node prototype, peer discovery, node communication, and distributed data experiments.",
	},
	{
		label: "In active development",
		tone: "blue",
		items: "Mission Control foundations, Data Parcels, archive health, and recovery validation.",
	},
	{
		label: "Research direction",
		tone: "plum",
		items: "Public Agent distribution, Mission Swarms, and humanity-scale applications.",
	},
] as const;

const systemComponents = [
	{
		name: "Mythadis Agent",
		status: "Research direction",
		tone: "plum",
		body: "The future participation layer for machines and operators performing bounded work. It is not public yet.",
	},
	{
		name: "Mission Control",
		status: "In active development",
		tone: "blue",
		body: "The coordination surface for making distributed work legible: missions, Signals, Relics, Annals, roles, and validation state.",
	},
] as const;

const missionControlTerms = [
	{
		name: "Signals",
		body: "observations or events relevant to a mission.",
	},
	{
		name: "Relics",
		body: "durable evidence artifacts or validated outputs.",
	},
	{
		name: "Annals",
		body: "the persistent record of mission history.",
	},
];
---

<BaseLayout
	title="Mythadis | Coordinate Intelligence"
	description="Mythadis exists to coordinate intelligence—not just computation. A proof-first fabric for human and artificial intelligence."
>
	<div class="ds-shell">
		<SiteHeader
			items={navItems}
			brandLabel="Mythadis"
			brandSubLabel="Swarm"
			status="Private R&D"
		/>

		<main>
			<section class="home-hero" id="vision" aria-labelledby="page-title">
				<div class="home-hero__copy">
					<SectionLabel>Mission question</SectionLabel>
					<h1 id="page-title">
						How do we{" "}<span class="hero-phrase">organize intelligence</span>&mdash;<span class="hero-phrase">human and artificial</span>&mdash;<span class="hero-phrase">so that together</span>{" "}
						we can{" "}<span class="hero-phrase">solve problems</span>{" "}that <span>neither could solve alone</span>?
					</h1>
					<p class="hero-answer">Mythadis exists to coordinate intelligence&mdash;not just computation.</p>
					<div class="hero-actions" aria-label="Primary page actions">
						<Button href="#problem">Explore the Vision</Button>
						<Button href="/current-state/" variant="secondary">Current State</Button>
						<Button href="#collaborate" variant="secondary">Collaborate</Button>
					</div>
				</div>
				<div class="home-hero__visual">
					<NetworkMotif />
				</div>
			</section>

			<section class="signal-statement" aria-label="Core system statement">
				<p>Many minds.</p>
				<p>Many machines.</p>
				<p>Many models.</p>
				<p>One proof-first fabric.</p>
			</section>

			<section class="problem-section" id="problem" aria-labelledby="problem-title">
				<div class="section-heading">
					<SectionLabel>Humanity's problem</SectionLabel>
					<h2 id="problem-title">The intelligence we need is fragmented.</h2>
				</div>
				<div class="problem-layout">
					<div class="fragment-map" aria-label="Fragmented systems diagram">
						{fragmentationNodes.map((node, index) => (
							<div class={`fragment-node fragment-node--${index + 1}`}>
								<span>{node}</span>
							</div>
						))}
						<div class="fragment-core">
							<span>Disconnected proof</span>
						</div>
					</div>
					<div class="problem-copy">
						<p>
							Knowledge, institutions, AI, people, and machines are learning in isolation.
						</p>
						<p>
							Mythadis asks what becomes possible when intelligence can discover, coordinate,
							verify, and remember together.
						</p>
					</div>
				</div>
			</section>

			<section class="swarm-section" id="swarm" aria-labelledby="swarm-title">
				<div class="section-heading">
					<SectionLabel>What is Mythadis Swarm?</SectionLabel>
					<h2 id="swarm-title">An emerging proof-first fabric for coordinated intelligence.</h2>
				</div>
				<div class="swarm-layout">
					<div class="swarm-copy">
						<p class="principle-line">Unity without centralization.</p>
						<p>
							Mythadis Swarm is the flagship research platform at the center of Mythadis:
							a private prototype for coordinating people, machines, models, evidence, and
							archives around mission-specific work.
						</p>
						<p>
							The goal is coordination, not central ownership. Participants should retain
							local agency while shared work becomes discoverable, bounded, testable, and
							remembered.
						</p>
					</div>
					<div class="status-model" aria-label="Mythadis status model">
						{statusModel.map((item) => (
							<article>
								<StatusLabel tone={item.tone}>{item.label}</StatusLabel>
								<p>{item.items}</p>
							</article>
						))}
					</div>
				</div>
			</section>

			<section class="flow-section" aria-labelledby="flow-title">
				<div class="section-heading">
					<SectionLabel>How the Swarm Works</SectionLabel>
					<h2 id="flow-title">A simple path from mission to recorded proof.</h2>
				</div>
				<ol class="flow-list" aria-label="Six-step Mythadis Swarm flow">
					{flowSteps.map((step) => (
						<li>
							<span>{step.name}</span>
							<p>{step.description}</p>
						</li>
					))}
				</ol>
			</section>

			<section class="components-section" aria-labelledby="components-title">
				<div class="section-heading">
					<SectionLabel>System components</SectionLabel>
					<h2 id="components-title">Participation at the edge. Legibility at the center.</h2>
				</div>
				<div class="components-grid">
					<div class="component-list">
						{systemComponents.map((component) => (
							<article>
								<StatusLabel tone={component.tone}>{component.status}</StatusLabel>
								<h3>{component.name}</h3>
								<p>{component.body}</p>
							</article>
						))}
					</div>
					<div class="control-panel" aria-label="Mission Control terminology">
						<div>
							<span>Mission</span>
							<p>a defined problem, scope, and proof objective.</p>
						</div>
						{missionControlTerms.map((term) => (
							<div>
								<span>{term.name}</span>
								<p>{term.body}</p>
							</div>
						))}
					</div>
				</div>
			</section>

			<section class="future-proof-section" aria-labelledby="future-proof-title">
				<div class="section-heading">
					<SectionLabel>Proof and future direction</SectionLabel>
					<h2 id="future-proof-title">Evidence before legend.</h2>
				</div>
				<div class="future-proof-layout">
					<div class="evidence-lines">
						<p>Claims require recorded observations, results, and validation material.</p>
						<p>Dissent and uncertainty should remain visible in the record.</p>
						<p>Proof discipline decides when a future claim deserves belief.</p>
					</div>
					<div class="mission-card-grid" aria-label="Research direction mission areas">
						{missionSwarms.map((mission) => (
							<article>
								<StatusLabel tone="plum">Research direction</StatusLabel>
								<h3>{mission}</h3>
							</article>
						))}
					</div>
				</div>
				<div class="future-proof-note">
					<p>Vision is not enough.</p>
					<p>Mission Swarms point toward domains where coordinated intelligence may matter, not demonstrated public outcomes.</p>
				</div>
			</section>

			<section class="current-state-section" id="current-state" aria-labelledby="state-title">
				<div class="section-heading">
					<SectionLabel>Current State</SectionLabel>
					<h2 id="state-title">The complete status lives on Current State.</h2>
				</div>
				<SurfacePanel variant="warning">
					<div class="state-summary">
						<ul aria-label="Homepage current state summary">
							{statusSummary.map((item) => <li>{item}</li>)}
						</ul>
						<p>
							Mythadis Swarm is private R&D with early multi-node prototype evidence. It is
							not a public production network.
						</p>
						<Button href="/current-state/">View the complete current state</Button>
					</div>
				</SurfacePanel>
			</section>

			<section class="collaborate-section" id="collaborate" aria-labelledby="collaborate-title">
				<SurfacePanel variant="strong">
					<div class="collaborate-card">
						<div>
							<SectionLabel>Collaborate</SectionLabel>
							<h2 id="collaborate-title">Help shape the first credible swarm.</h2>
							<p>
								Early conversations are most useful with people who can pressure-test the
								architecture, the proof discipline, and the mission model.
							</p>
						</div>
						<div class="collaborator-list" aria-label="Collaboration audiences">
							{collaborators.map((collaborator) => <span>{collaborator}</span>)}
						</div>
						<div class="contact-panel">
							<p class="contact-label">Engagement paths</p>
							<a href="https://ie.linkedin.com/in/mrblanzy" rel="noreferrer">
								Start a conversation
							</a>
							<a href="https://github.com/blanzy-labs" rel="noreferrer">
								Explore Blanzy Labs on GitHub
							</a>
							<p>
								LinkedIn is the current direct conversation path. GitHub remains the public
								evidence path for technical work while Mythadis is private R&D.
							</p>
						</div>
					</div>
				</SurfacePanel>
			</section>
		</main>

		<footer class="public-footer">
			<p>
				Mythadis is an independent research and engineering initiative from Blanzy Labs,
				founded by Rob Blanzy.
			</p>
			<nav aria-label="Footer navigation">
				<a href="https://ie.linkedin.com/in/mrblanzy" rel="noreferrer">Rob Blanzy</a>
				<a href="https://github.com/blanzy-labs" rel="noreferrer">Blanzy Labs</a>
				<a href="/field-reports/">Field Reports</a>
				<a href="/proof/">Proof</a>
				<a href="/current-state/">Current State</a>
			</nav>
		</footer>
	</div>
</BaseLayout>

<style>
	h1,
	h2,
	h3,
	p {
		text-wrap: pretty;
	}

	h1,
	h2,
	h3 {
		margin: 0;
		font-family: var(--ds-font-display);
		letter-spacing: 0;
	}

	.home-hero {
		display: grid;
		grid-template-columns: minmax(0, 1.15fr) minmax(280px, 0.85fr);
		gap: clamp(24px, 3.4vw, 48px);
		align-items: center;
		min-height: min(720px, calc(100svh - 84px));
		padding: clamp(16px, 2.6svh, 30px) 0 clamp(32px, 4.4svh, 58px);
	}

	.home-hero__copy {
		max-width: 800px;
		position: relative;
		z-index: 1;
	}

	.home-hero h1 {
		max-width: 22.5ch;
		margin-top: 10px;
		font-size: clamp(2.7rem, 3.95vw, 3.8rem);
		line-height: 0.98;
		text-wrap: balance;
	}

	@media (min-width: 681px) {
		.home-hero h1 .hero-phrase {
			white-space: nowrap;
		}
	}

	.home-hero__visual {
		width: min(100%, 520px, 50svh);
		justify-self: end;
		align-self: center;
	}

	.hero-answer {
		max-width: 620px;
		margin: clamp(16px, 2.3svh, 24px) 0 0;
		color: var(--ds-ink);
		font-family: var(--ds-font-display);
		font-size: clamp(1.16rem, 1.48vw, 1.58rem);
		font-weight: 700;
		line-height: 1.15;
	}

	.hero-actions {
		display: flex;
		flex-wrap: wrap;
		gap: 14px;
		margin-top: clamp(18px, 2.6svh, 26px);
	}

	.signal-statement {
		display: grid;
		grid-template-columns: repeat(4, 1fr);
		gap: 0;
		margin: 0 0 118px;
		border-block: 1px solid var(--ds-line);
	}

	.home-hero + .signal-statement {
		margin-top: 8px;
	}

	.signal-statement p {
		min-height: 148px;
		margin: 0;
		padding: 22px;
		border-right: 1px solid var(--ds-line);
		color: var(--ds-ink);
		font-family: var(--ds-font-display);
		font-size: clamp(1.45rem, 2.6vw, 2.7rem);
		font-weight: 700;
		line-height: 1;
	}

	.signal-statement p:last-child {
		border-right: 0;
		color: var(--ds-mint);
	}

	.problem-section,
	.swarm-section,
	.flow-section,
	.components-section,
	.future-proof-section,
	.agent-section,
	.mission-control-section,
	.mission-swarms-section,
	.current-state-section,
	.collaborate-section {
		padding: 0 0 118px;
	}

	.section-heading {
		max-width: 900px;
		margin-bottom: 30px;
	}

	.section-heading h2,
	.components-section h2,
	.agent-section h2,
	.control-copy h2,
	.collaborate-card h2 {
		font-size: clamp(2.2rem, 4.6vw, 4.55rem);
		line-height: 0.98;
	}

	.problem-layout,
	.swarm-layout,
	.components-grid,
	.future-proof-layout,
	.agent-section,
	.control-grid,
	.state-panel,
	.collaborate-card {
		display: grid;
		grid-template-columns: minmax(0, 0.92fr) minmax(320px, 1fr);
		gap: 32px;
		align-items: start;
	}

	.fragment-map {
		position: relative;
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 12px;
		min-height: 390px;
		padding: 22px;
		border: 1px solid var(--ds-line-strong);
		background:
			linear-gradient(135deg, rgba(255, 255, 255, 0.06), transparent 28%),
			radial-gradient(circle at 50% 50%, rgba(66, 217, 255, 0.18), transparent 13rem),
			rgba(2, 7, 10, 0.46);
		overflow: hidden;
	}

	.fragment-map::before,
	.fragment-map::after {
		position: absolute;
		inset: 50% 10%;
		height: 1px;
		content: "";
		background: linear-gradient(90deg, transparent, rgba(66, 217, 255, 0.48), transparent);
	}

	.fragment-map::after {
		transform: rotate(90deg);
	}

	.fragment-node,
	.fragment-core,
	.collaborator-list span {
		border: 1px solid var(--ds-line);
		background: rgba(10, 22, 28, 0.74);
	}

	.fragment-node {
		position: relative;
		z-index: 1;
		display: grid;
		align-items: center;
		min-height: 98px;
		padding: 14px;
		color: var(--ds-ink-soft);
		font-family: var(--ds-font-mono);
		font-size: 0.72rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
	}

	.fragment-node--2,
	.fragment-node--5 {
		transform: translateY(34px);
	}

	.fragment-core {
		position: absolute;
		z-index: 2;
		top: 50%;
		left: 50%;
		display: grid;
		place-items: center;
		width: min(52%, 250px);
		min-height: 86px;
		padding: 16px;
		color: var(--ds-amber);
		font-family: var(--ds-font-display);
		font-weight: 700;
		text-align: center;
		transform: translate(-50%, -50%);
		box-shadow: 0 0 36px rgba(246, 197, 107, 0.12);
	}

	.problem-copy {
		display: grid;
		gap: 18px;
	}

	.problem-copy p,
	.swarm-copy p,
	.status-model p,
	.component-list p,
	.control-panel p,
	.future-proof-note p,
	.state-summary p,
	.swarm-grid p,
	.agent-panel p,
	.control-copy p,
	.state-copy p,
	.collaborate-card p,
	.contact-panel p {
		margin: 0;
		color: var(--ds-ink-soft);
		font-size: 1rem;
		line-height: 1.68;
	}

	.problem-copy p:first-child {
		color: var(--ds-ink);
		font-family: var(--ds-font-display);
		font-size: clamp(1.55rem, 3vw, 2.45rem);
		font-weight: 700;
		line-height: 1.08;
	}

	.unity-section {
		display: grid;
		align-content: center;
		min-height: 74svh;
		padding: 76px 0 132px;
	}

	.unity-section h2 {
		max-width: 920px;
		font-size: clamp(4.4rem, 11vw, 10.8rem);
		line-height: 0.86;
	}

	.unity-section p {
		max-width: 620px;
		margin: 30px 0 0 auto;
		color: var(--ds-ink-soft);
		font-size: clamp(1.05rem, 1.8vw, 1.28rem);
		line-height: 1.65;
	}

	.swarm-layout {
		align-items: stretch;
	}

	.swarm-copy {
		display: grid;
		gap: 18px;
		align-content: start;
	}

	.principle-line {
		color: var(--ds-ink);
		font-family: var(--ds-font-display);
		font-size: clamp(2rem, 4.5vw, 4.9rem);
		font-weight: 700;
		line-height: 0.94;
	}

	.status-model,
	.component-list {
		display: grid;
		gap: 14px;
	}

	.status-model article,
	.component-list article {
		padding: 22px;
		border: 1px solid var(--ds-line);
		background: var(--ds-surface);
	}

	.status-model p {
		margin-top: 16px;
	}

	.swarm-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 18px;
	}

	.swarm-grid article,
	.mission-card-grid article {
		min-height: 292px;
		padding: 24px;
		border: 1px solid var(--ds-line);
		background: var(--ds-surface);
		box-shadow: 0 18px 54px rgba(0, 0, 0, 0.2);
	}

	.swarm-grid h3,
	.mission-card-grid h3 {
		margin-top: 34px;
		font-size: clamp(1.45rem, 2.6vw, 2.3rem);
		line-height: 1.04;
	}

	.swarm-grid p {
		margin-top: 16px;
	}

	.flow-list {
		counter-reset: flow-step;
		display: grid;
		grid-template-columns: repeat(6, 1fr);
		gap: 0;
		padding: 0;
		margin: 0;
		list-style: none;
		border: 1px solid var(--ds-line-strong);
		background: rgba(2, 7, 10, 0.36);
	}

	.flow-list li {
		counter-increment: flow-step;
		position: relative;
		display: grid;
		align-content: space-between;
		min-height: 248px;
		padding: 20px;
		border-right: 1px solid var(--ds-line);
	}

	.flow-list li:last-child {
		border-right: 0;
	}

	.flow-list li:not(:last-child)::after {
		position: absolute;
		top: 50%;
		right: -9px;
		z-index: 2;
		width: 18px;
		height: 18px;
		content: "";
		border-top: 1px solid var(--ds-blue);
		border-right: 1px solid var(--ds-blue);
		background: var(--ds-void);
		transform: translateY(-50%) rotate(45deg);
	}

	.flow-list li::before {
		color: var(--ds-blue);
		font-family: var(--ds-font-mono);
		font-size: 0.7rem;
		content: "0" counter(flow-step);
	}

	.flow-list span {
		font-family: var(--ds-font-display);
		font-size: clamp(1.12rem, 1.7vw, 1.5rem);
		font-weight: 700;
		line-height: 1.05;
	}

	.flow-list p {
		margin: 18px 0 0;
		color: var(--ds-ink-soft);
		font-size: 0.92rem;
		line-height: 1.55;
	}

	.components-grid {
		grid-template-columns: minmax(0, 0.95fr) minmax(320px, 1fr);
		align-items: stretch;
	}

	.component-list h3 {
		margin-top: 22px;
		font-size: clamp(1.65rem, 2.8vw, 2.5rem);
		line-height: 1.02;
	}

	.component-list p {
		margin-top: 16px;
	}

	.agent-panel,
	.state-panel,
	.collaborate-card {
		padding: 28px;
	}

	.agent-panel p {
		margin-top: 24px;
		font-family: var(--ds-font-display);
		font-size: clamp(1.45rem, 2.8vw, 2.4rem);
		font-weight: 700;
		line-height: 1.08;
	}

	.control-grid {
		grid-template-columns: minmax(320px, 0.82fr) minmax(0, 1fr);
		align-items: stretch;
	}

	.control-copy p {
		margin-top: 24px;
	}

	.control-panel {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 12px;
		padding: 22px;
		border: 1px solid var(--ds-line-strong);
		background:
			linear-gradient(135deg, rgba(255, 255, 255, 0.06), transparent 30%),
			rgba(2, 7, 10, 0.48);
	}

	.control-panel div {
		display: grid;
		align-content: start;
		min-height: 142px;
		padding: 18px;
		border: 1px solid var(--ds-line);
		background: rgba(10, 22, 28, 0.74);
	}

	.control-panel span {
		color: var(--ds-ink);
		font-family: var(--ds-font-display);
		font-size: clamp(1.28rem, 2.4vw, 2rem);
		font-weight: 700;
	}

	.control-panel p {
		margin-top: 12px;
	}

	.mission-card-grid {
		display: grid;
		grid-template-columns: repeat(5, 1fr);
		gap: 14px;
	}

	.mission-card-grid article {
		position: relative;
		min-height: 192px;
		overflow: hidden;
	}

	.mission-card-grid article::before {
		position: absolute;
		inset: 18px;
		content: "";
		border: 1px solid rgba(189, 238, 255, 0.12);
	}

	.mission-card-grid span {
		display: block;
		width: 36px;
		height: 36px;
		border: 1px solid var(--ds-line-strong);
		background: linear-gradient(135deg, var(--ds-blue), var(--ds-mint));
		box-shadow: 0 0 28px rgba(66, 217, 255, 0.28);
	}

	.future-proof-section {
		margin: 0 0 118px;
		padding: 76px 0;
		border-block: 1px solid var(--ds-line-strong);
		background:
			linear-gradient(90deg, rgba(66, 217, 255, 0.08), transparent 34%),
			linear-gradient(180deg, rgba(2, 7, 10, 0.18), rgba(2, 7, 10, 0.58));
	}

	.future-proof-layout {
		grid-template-columns: minmax(0, 0.75fr) minmax(320px, 1fr);
	}

	.evidence-section {
		margin: 0 0 118px;
		padding: 76px 0;
		border-block: 1px solid var(--ds-line-strong);
		background:
			linear-gradient(90deg, rgba(66, 217, 255, 0.08), transparent 34%),
			linear-gradient(180deg, rgba(2, 7, 10, 0.18), rgba(2, 7, 10, 0.58));
	}

	.evidence-section h2 {
		max-width: 920px;
		font-size: clamp(3.6rem, 9vw, 8.4rem);
		line-height: 0.86;
	}

	.evidence-lines {
		display: grid;
		grid-template-columns: 1fr;
		gap: 18px;
		margin-top: 0;
	}

	.evidence-lines p {
		margin: 0;
		padding-top: 18px;
		border-top: 1px solid var(--ds-line);
		color: var(--ds-ink-soft);
		font-family: var(--ds-font-display);
		font-size: clamp(1.35rem, 2.4vw, 2.05rem);
		font-weight: 700;
		line-height: 1.08;
	}

	.future-proof-note {
		display: flex;
		flex-wrap: wrap;
		gap: 18px;
		margin-top: 36px;
	}

	.future-proof-note p:first-child {
		color: var(--ds-mint);
		font-family: var(--ds-font-display);
		font-size: clamp(1.7rem, 3vw, 3rem);
		font-weight: 700;
		line-height: 1;
	}

	.state-panel h3 {
		margin-top: 24px;
		font-size: clamp(1.6rem, 2.8vw, 2.5rem);
		line-height: 1.02;
	}

	.state-copy {
		display: grid;
		gap: 16px;
	}

	.state-summary {
		display: grid;
		gap: 22px;
		padding: 28px;
	}

	.state-summary ul {
		display: grid;
		grid-template-columns: repeat(4, 1fr);
		gap: 10px;
		padding: 0;
		margin: 0;
		list-style: none;
	}

	.state-summary li {
		padding: 12px 14px;
		border: 1px solid var(--ds-line);
		color: var(--ds-ink-soft);
		background: rgba(10, 22, 28, 0.64);
		font-family: var(--ds-font-mono);
		font-size: 0.68rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
	}

	.state-summary :global(.ds-button) {
		justify-self: start;
	}

	.collaborate-card {
		grid-template-columns: minmax(0, 1fr) minmax(240px, 0.55fr) minmax(300px, 0.72fr);
	}

	.collaborate-card p {
		margin-top: 18px;
	}

	.collaborator-list {
		display: grid;
		gap: 10px;
	}

	.collaborator-list span {
		padding: 12px 14px;
		color: var(--ds-ink-soft);
		font-family: var(--ds-font-mono);
		font-size: 0.72rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
	}

	.contact-panel {
		padding: 18px;
		border: 1px solid var(--ds-line);
		background: rgba(2, 7, 10, 0.32);
	}

	.contact-label {
		margin: 0 0 12px;
		color: var(--ds-blue);
		font-family: var(--ds-font-mono);
		font-size: 0.68rem;
		text-transform: uppercase;
		letter-spacing: 0.08em;
	}

	.contact-panel a {
		display: flex;
		margin-bottom: 12px;
		color: var(--ds-ink);
		font-family: var(--ds-font-display);
		font-size: 1.2rem;
		font-weight: 700;
		border-bottom: 1px solid var(--ds-blue);
	}

	.contact-panel p {
		margin-top: 0;
	}

	.public-footer {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		align-items: start;
		justify-content: space-between;
		gap: 20px;
		padding: 34px 0 46px;
		border-top: 1px solid var(--ds-line);
		color: var(--ds-ink-muted);
		font-size: 0.86rem;
	}

	.public-footer p {
		margin: 0;
	}

	.public-footer nav {
		display: flex;
		flex-wrap: wrap;
		gap: 18px;
	}

	.public-footer a {
		color: var(--ds-ink-soft);
		border-bottom: 1px solid var(--ds-line-strong);
	}

	@media (max-width: 1040px) {
		.home-hero,
		.problem-layout,
		.swarm-layout,
		.components-grid,
		.future-proof-layout,
		.agent-section,
		.control-grid,
		.state-panel,
		.collaborate-card {
			grid-template-columns: 1fr;
		}

		.home-hero {
			min-height: auto;
			gap: 38px;
			padding-top: 24px;
		}

		.home-hero__copy {
			max-width: 820px;
		}

		.home-hero h1 {
			max-width: 20ch;
			font-size: clamp(2.95rem, 7.5vw, 4.75rem);
		}

		.home-hero__visual {
			width: min(100%, 520px, 46svh);
			justify-self: start;
		}

		.signal-statement,
		.swarm-grid,
		.state-summary ul {
			grid-template-columns: repeat(2, 1fr);
		}

		.flow-list,
		.mission-card-grid {
			grid-template-columns: repeat(3, 1fr);
		}

		.flow-list li {
			border-bottom: 1px solid var(--ds-line);
		}

		.flow-list li:nth-child(3n) {
			border-right: 0;
		}

		.flow-list li:not(:last-child)::after {
			display: none;
		}
	}

	@media (min-width: 981px) and (max-width: 1040px) {
		.home-hero {
			grid-template-columns: minmax(0, 1.12fr) minmax(280px, 0.88fr);
			gap: 28px;
			align-items: center;
			min-height: calc(100svh - 84px);
			padding-top: 18px;
			padding-bottom: 34px;
		}

		.home-hero h1 {
			max-width: 20.5ch;
			font-size: clamp(2.7rem, 3.95vw, 4.2rem);
		}

		.home-hero__visual {
			width: min(100%, 430px, 48svh);
			justify-self: end;
		}
	}

	@media (max-width: 680px) {
		.home-hero {
			gap: 28px;
			padding-top: 14px;
			padding-bottom: 48px;
		}

		.home-hero h1 {
			max-width: 100%;
			font-size: clamp(2.05rem, 9.2vw, 3.05rem);
			line-height: 1.02;
		}

		.home-hero h1 span {
			white-space: normal;
		}

		.home-hero__visual {
			width: min(100%, 390px, 36svh);
		}

		.hero-answer {
			font-size: clamp(1.08rem, 4.7vw, 1.38rem);
		}

		.hero-actions,
		.public-footer {
			flex-direction: column;
		}

		.hero-actions :global(.ds-button) {
			width: 100%;
		}

		.signal-statement,
		.swarm-grid,
		.flow-list,
		.mission-card-grid,
		.state-summary ul {
			grid-template-columns: 1fr;
		}

		.signal-statement {
			margin-bottom: 76px;
		}

		.signal-statement p {
			min-height: 92px;
			border-right: 0;
			border-bottom: 1px solid var(--ds-line);
		}

		.signal-statement p:last-child {
			border-bottom: 0;
		}

		.problem-section,
		.swarm-section,
		.flow-section,
		.components-section,
		.future-proof-section,
		.agent-section,
		.mission-control-section,
		.mission-swarms-section,
		.current-state-section,
		.collaborate-section {
			padding-bottom: 76px;
		}

		.section-heading h2,
		.components-section h2,
		.agent-section h2,
		.control-copy h2,
		.collaborate-card h2 {
			font-size: clamp(2rem, 11vw, 3.4rem);
		}

		.fragment-map {
			grid-template-columns: 1fr;
			min-height: auto;
		}

		.fragment-node--2,
		.fragment-node--5 {
			transform: none;
		}

		.fragment-core {
			position: relative;
			top: auto;
			left: auto;
			width: 100%;
			transform: none;
		}

		.unity-section {
			min-height: auto;
			padding: 22px 0 88px;
		}

		.unity-section h2,
		.evidence-section h2 {
			font-size: clamp(2.45rem, 10.8vw, 3.6rem);
			overflow-wrap: anywhere;
		}

		.unity-section p {
			margin-left: 0;
		}

		.status-model article,
		.component-list article,
		.swarm-grid article,
		.mission-card-grid article,
		.flow-list li {
			min-height: 178px;
		}

		.flow-list li {
			border-right: 0;
		}

		.control-panel {
			grid-template-columns: 1fr;
		}

		.evidence-section {
			margin-bottom: 76px;
			padding: 56px 0;
		}

		.future-proof-section {
			margin-bottom: 76px;
			padding: 56px 0;
		}

		.agent-panel,
		.state-panel,
		.state-summary,
		.collaborate-card {
			padding: 20px;
		}
	}
</style>
